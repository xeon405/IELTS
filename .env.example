# Required for real Gemini AI evaluation. Without it, the AI Brain routes
# automatically fall back to the deterministic local examiner logic.
# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# Optional: model name. Defaults to gemini-3.6-flash.
GEMINI_MODEL="gemini-3.6-flash"
