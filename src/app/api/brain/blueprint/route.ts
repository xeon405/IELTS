import { NextResponse } from "next/server";import { getListeningBlueprint, getReadingBlueprint, getSpeakingBlueprint, getWritingBlueprint, type Skill } from "@/lib/ielts-brain";export async function GET(request: Request) {  const url = new URL(request.url);  const module = url.searchParams.get("module") as Skill | null;  if (!module) {    return NextResponse.json({ error: "A module query parameter is required." }, { status: 400 });  }  const blueprint =    module === "listening"      ? getListeningBlueprint()      : module === "writing"        ? getWritingBlueprint()        : module === "speaking"          ? getSpeakingBlueprint()          : getReadingBlueprint();  return NextResponse.json(blueprint);}