import { NextResponse } from "next/server";import {  createDefaultLearningProfile,  evaluatePracticeSession,  type PracticeSession,  type StudentLearningProfile,} from "@/lib/ielts-brain";export async function POST(request: Request) {  const body = await request.json().catch(() => ({}));  const profile = {    ...createDefaultLearningProfile(),    ...(body.profile && typeof body.profile === "object" ? body.profile : {}),  } as StudentLearningProfile;  const session = body.session as PracticeSession | undefined;  const answers = (body.answers ?? {}) as Record<string, string>;  const timing = (body.timing ?? {}) as { totalSeconds?: number | null };  if (!session) {    return NextResponse.json({ error: "Practice session is required." }, { status: 400 });  }  const normalized = {    questionTypes: [],    examinerIntent: "",    title: "",    difficultyBand: 6,    ...(session as object),  } as unknown as PracticeSession;  return NextResponse.json(evaluatePracticeSession(profile, normalized, answers, timing.totalSeconds));}